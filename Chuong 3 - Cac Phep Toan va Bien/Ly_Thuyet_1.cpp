#include <iostream>
#include <cmath>

// 1. Cú pháp (syntax):
//      - toán tử (operator): kí hiệu, kí tự --> toán hạng
//      - hàm (function): kí tự (đối số)
// 2. Số toán hạng (operand) tham gia (ngôi)
// 3. Độ ưu tiên (precedence)
// 4. Thứ tự thực hiện (asociativity)
// 5. Giá trị/Kết quả trả về (return value)

int main ()
{
    bool x = 1234;
    std::cout << x;
}
